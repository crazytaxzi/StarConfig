# Windows Build Verification

This branch exists only to force a pull-request Windows build of the current usable StarConfig application. Remove this file after verification.
# Build Verification

This file marks the first complete Windows application build.
